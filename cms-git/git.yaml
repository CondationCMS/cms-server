## YAML Template.
---
repos:
    - name: "test-repo"
      credentials: 
        username: thmarx
        password: "pwd or token"
      uri: "git repo"
      folder: "target/sites/target"
      branch: "release"
      cron: "0/10 * * * * ?"