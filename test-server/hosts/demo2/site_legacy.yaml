hostname: localhost3
baseurl: "http://localhost3:2020"
language: en
template:
  engine: pebble
modules:
  active:
    - pebble-module
content:
  pipeline:
    - template
    - markdown
    - shortcode